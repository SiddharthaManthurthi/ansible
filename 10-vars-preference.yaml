- name: variable preference 
  hosts: local
  connection: local
  vars:
   NAME: "Ansible from playbook"
  vars_files:
  - course.yaml
  vars_prompt:
  - name: NAME
    prompt: please enter your name
    private: false
  tasks:
  - name: print name
    # vars:
    #  NAME: "Ansible from TASK"
    ansible.builtin.debug:
     msg: " Hi {{ NAME }}"

    # 1. args first preference
    # 2. task
